#
# orbstack - Sources the OrbStack init script
#

ORBSTACK_SCRIPT_PATH="$HOME/.orbstack/shell/init.zsh"
[[ -s "$ORBSTACK_SCRIPT_PATH" ]] && source "$ORBSTACK_SCRIPT_PATH"

# vim: ft=zsh sw=2 ts=2 et
